import React from 'react'
import {data} from '../utils/Data'
const TestPage = () => {
    console.log(data);
    console.log(typeof data);
    return (
        <div>
            testpage
        </div>
    )
}

export default TestPage
